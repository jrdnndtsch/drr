<%= form_for [:admin, @clinical_trial] do |f| %>
  <% if @clinical_trial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clinical_trial.errors.count, "error") %> prohibited this clinical_trial from being saved:</h2>

      <ul>
      <% @clinical_trial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :pdf %><br>
    <%= f.file_field :pdf %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
