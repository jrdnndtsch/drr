<%= form_for [:admin, @learning_module] do |f| %>
  <% if @learning_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@learning_module.errors.count, "error") %> prohibited this learning_module from being saved:</h2>

      <ul>
      <% @learning_module.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <%= f.label :image %>
  <%= f.file_field :image %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
